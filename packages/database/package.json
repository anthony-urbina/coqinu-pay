{
  "name": "database",
  "main": "./index.ts",
  "types": "./index.ts",
  "dependencies": {
    "@prisma/client": "^5.6.0"
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "prisma": "^5.6.0"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate"
  }
}
